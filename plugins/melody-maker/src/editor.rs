// Editor will be added in next iteration
// For now, parameters are accessible via host automation

pub fn create(_params: std::sync::Arc<crate::MelodyMakerParams>) -> Option<Box<dyn nih_plug::prelude::Editor>> {
    None
}
